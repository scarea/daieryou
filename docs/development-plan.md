# Daieryou 开发计划与任务追踪

> 目的：防止对话中断或上下文丢失时，开发计划、任务状态、验收口径不可追溯。  
> 更新时间：2026-03-07

## 0. 北极星目标
- 先把服务端从“可玩原型”提升到“可稳态运行”，优先解决 OOM、状态一致性、身份安全。
- 在不破坏现有玩法和交互的前提下，逐步引入 Redis 热状态能力，最终可切主存。

## 1. 里程碑
### M1 - 安全与稳定基线（P0）
- [x] 会话从可伪造 `userId` 改为 `sessionToken`（签发/验签/过期）
- [x] WS `maxPayload` 限制
- [x] 单连接请求限流（429）
- [x] 请求幂等缓存（`operationId`）
- [x] `selectCards` 业务幂等（重试不脏写）
- [x] 用户档案缓存上限（防无界增长）

### M2 - 热状态容灾（P0）
- [x] Redis 房间镜像写入（save/delete）
- [x] 启动恢复（Redis -> 内存）
- [x] 周期增量恢复（only missing）
- [x] 按请求 `roomId` 读通路回源
- [x] 登录/大厅按需 warm sync
- [x] 切换为 Redis 主写灰度模式（内存仅本地缓存，失败自动降级并重试）

### M3 - 可观测与运维（P1）
- [ ] 核心指标：在线连接数、房间数、恢复数、限流命中、幂等命中
- [x] 网关结构化日志与请求追踪 ID
- [ ] CI 门禁：server test + e2e + build

## 2. 本轮已落地（代码事实）
- `sessionToken` 鉴权与恢复链路已完成（前后端+测试）。
- 网关已具备限流与幂等缓存。
- 网关已统一错误响应（`400/404/429/500`）并附带 `traceId`，避免前端超时才感知失败。
- 入站协议已接入基础校验（`id/route/body` 类型与长度）。
- 入站协议已接入按路由字段白名单校验，并记录网关命中计数（校验拒绝/限流/幂等命中）。
- 房间写路径已支持 Redis 主写灰度模式（`primaryMirrorWrites`），镜像失败自动降级本地写并进入重试队列。
- 战绩总览 MVP 已接入（近 N 局记录、胜率、总分变化）。
- 战绩数据已持久化到 Mongo（进程重启不丢），并保留内存降级兜底。
- 战绩查询增强已落地（分页、时间筛选、按房间/名次过滤）。
- 战绩治理能力已接入（按时间窗口归档/清理，默认灰度关闭）与索引巡检脚本。
- 规则比较与结算内存释放修复。
- Redis 镜像组件、仓储钩子、启动恢复、增量恢复、按需回源已完成。
- 邮箱注册/登录与验证码链路已接入（后端接口 + 前端页面 + 测试）。
- 邀请码注册链路修复：`entryHandler.register` 已透传 `inviteCode`，并修复“邀请码失败导致验证码被提前消费”的问题。
- 会员体系 MVP 已接入：自助开通/续费、管理员发放会员、邀请码运营列表与禁用。
- 管理审计日志 MVP 已接入：关键运营动作写审计日志，管理端可分页查询。
- 大厅前端已接入会员中心与管理台（最小版），并通过回归基线（server test/e2e/build）。
- 已补规则护栏：已出牌不会回流到手牌或牌堆，并补充“整副牌 54 张唯一性”回归测试。
- 已补 AI 约束：bot 决策输入接入 `knownRemovedCards`，策略模拟剔除已打出牌。
- 已补前端热修：改为玩家视角（手牌区在下方），并优化为默认视口内可操作（无需滚动）+ 公牌清晰可见，修复“无法出牌/看不清公牌”。
- 已补核心规则：大小王按赖子处理，结算时自动推导当前可达的最大牌型并参与比牌。

## 3. 当前风险清单（按优先级）
1. **高**：Redis 主写能力已支持灰度开关，但默认未全量启用；多实例一致性仍需压测校准与上线切流策略。
2. **中**：`RoomRepository` 已有 `maxInMemoryRooms` 上限，但阈值需要结合压测校准。
3. **中**：缺少运行时观测指标，不利于提前发现 OOM 前兆。
4. **中**：前端主包体积偏大，冷启动成本高。
5. **低**：账号/战绩体系已切到 Mongo，后续仍需补充迁移脚本与历史数据演练方案。
6. **中**：AI 人机若直接走大模型实时决策会带来时延/成本/稳定性风险，需策略引擎兜底。

## 4. 下一批执行任务（短周期）
### T1（进行中）
- 目标：增加内存房间上限与淘汰策略，降低 OOM 风险。
- 验收：
  - 可配置 `maxInMemoryRooms`
  - 超限时自动淘汰低优先级房间（优先 finished、其次长时间 waiting）
  - 单测覆盖淘汰行为

### T1 进度更新（2026-03-06）
- [x] `maxInMemoryRooms` 配置与运行时解析已接入。
- [x] `RoomRepository` 已实现本地淘汰策略（不触发 Redis 镜像删除）。
- [x] 补充淘汰行为单测（优先级淘汰、同优先级按最旧淘汰）。

### T2（待开始）
- 目标：Redis 主热状态模式开关（灰度），读写优先 Redis，内存仅缓存。
- 验收：
  - 配置开关可控
  - 主要路径一致性测试通过
  - 失败可快速回退到当前“镜像模式”

### T2 进度更新（2026-03-06）
- [x] 增加 `preferMirrorReads` 配置开关（灰度入口）。
- [x] 周期同步与按需 warm sync 已支持覆盖已有内存房间（`onlyMissing=false`）。
- [x] `roomId` 请求路径支持“有缓存也可强制从镜像刷新”。
- [x] 增加镜像写失败重试队列（降低 Redis 短暂故障导致的数据缺口）。
- [x] 写路径已支持 Redis 主写灰度（`DAIERYOU_ROOM_MIRROR_PRIMARY_WRITES`），失败降级本地写并入重试队列。

### T3（待开始）
- 目标：观测指标接入与日志整理。
- 验收：
  - 指标输出
  - 限流/幂等/恢复命中可观测

### T3 进度更新（2026-03-07）
- [x] 网关错误响应统一并附带 `traceId`。
- [x] 入站协议基础校验（`id/route/body`）已接入。
- [x] 入站字段白名单校验与命中指标计数（限流/幂等/校验）已接入。
- [x] 网关结构化日志已接入（拒绝/失败/限流/路由不存在）。
- [ ] 指标上报（外部系统）仍待补齐。

### T4（已完成）
- 目标：会员与邀请码运营 MVP 打通（后端能力 + 前端最小管理台）。
- 验收：
  - [x] 会员自助开通/续费接口
  - [x] 管理员发放会员接口
  - [x] 邀请码运营字段（`channel/campaign/remark`）与后台列表/禁用
  - [x] 大厅会员中心与管理台最小面板
  - [x] 回归基线通过（server test + e2e + build）

### T5（进行中）
- 目标：战绩模块业务化第二阶段（在“战绩持久化完成”基础上补齐可运营能力）。
- 验收：
  - [x] 战绩查询增强：分页、时间筛选、按房间/名次过滤
  - [x] 战绩治理：归档/清理策略（按时间窗口）与索引巡检文档
  - [ ] 战绩查询性能基线：核心查询索引覆盖并输出压测/Explain 结果
  - [ ] 对外文档：战绩字段、查询参数、错误码与降级行为

### T5 进度更新（2026-03-07）
- [x] `getBattleStats` 已支持分页（`page/limit`）与筛选（`roomId/rank/startTime/endTime`）。
- [x] Mongo 查询链路已支持筛选分页与总量返回（`pagination.total/totalPages/hasMore`）。
- [x] 大厅战绩面板已接入筛选与分页 UI（房间、名次、起止日期、翻页）。
- [x] 单测与回归通过（`server test`、`client build`、`e2e`）。
- [x] 战绩治理服务已接入（按 `retentionMs` 窗口、分批归档/清理、灰度开关默认关闭）。
- [x] 索引巡检脚本与文档已补齐（`server/scripts/audit-battle-record-indexes.js`、`docs/battle-record-governance.md`）。
- [ ] 下一步聚焦：战绩查询性能基线（Explain + 压测口径）与对外字段文档。

### T6（进行中）
- 目标：AI 人机接入 MVP（先解决“等人开局”与“可持续陪练”）。
- 验收：
  - [ ] waiting 房间支持房主补 1-2 个 AI 玩家，补位后满足 3 人即可开局
  - [ ] AI 决策链采用可控策略引擎（规则/策略），并具备超时 fallback
  - [ ] AI 行为审计日志可追溯（`roomId/round/playerId/provider/latency/fallback`）
  - [ ] 接入灰度开关与运行时配置（`enabled/maxPerRoom/decisionTimeoutMs`）
  - [ ] 回归基线通过（server test + e2e + build）

### T6 进度更新（2026-03-07）
- [x] 完成 AI 人机接入 PRD 与架构拆分（`docs/ai-bot-prd.md`）。
- [x] 完成可执行任务拆分与验收口径（`docs/ai-bot-task-breakdown.md`）。
- [x] 后端已接入 bot 运行时开关（`bot.enabled/maxPerRoom/decisionTimeoutMs/defaultDifficulty`）。
- [x] 后端已接入 bot 决策服务与 fallback（`BotDecisionService` + `Rule/Fallback Provider`）。
- [x] 房间能力已接入房主补位/移除 AI（`addBots/removeBot`）。
- [x] 对局流程已接入 bot 自动决策（开局/新回合/玩家出牌后），超时托管仍保留兜底。
- [x] 前端“添加/移除 AI”控制入口与联调已完成（waiting 房间房主可操作，支持 AI 标签展示）。
- [x] 策略输入已接入 `knownRemovedCards`，AI 不会把已打出牌纳入未知牌池估算。
- [x] 已新增端到端断言：历史已出牌不会再次出现在后续手牌。
- [x] 已补齐结算规则：次名分别向头名/末位结算；豹子对豹子单笔翻倍（含赖子成豹）。
- [x] 已优化公牌表现：当前回合公牌高亮，其余公牌缩小弱化。

## 5. 业务 TODO（产品与玩法）
### B0 - 账号体系（高）
- [x] 邮箱验证码发送（支持 `console/webhook/resend` 通道）
- [x] 邮箱注册（验证码 + 密码）
- [x] 邮箱密码登录（稳定绑定 `sessionToken` 与 `userId`）
- [x] 会员自助开通/续费（MVP）
- [x] 管理员发放会员（MVP）
- [x] 邀请码运营最小闭环（生成/列表/禁用）
- [ ] 支付订单与回调（正式商业化闭环）
- [ ] 会员到期提醒与召回策略

### B1 - 对局稳定与公平（高）
- [ ] 托管机制：玩家超时未操作自动托管出牌
- [ ] 回合操作倒计时：统一超时策略，避免卡局
- [ ] 断线重连一致性：重连后明确“可操作/等待中”状态
- [ ] 随机可追溯：记录牌局 seed/发牌快照（用于争议排查）

### B2 - 用户体验与留存（中）
- [x] 战绩页：近 N 局结果、胜率、总分变化
- [x] 战绩持久化：Mongo 落库（进程重启不丢）+ 内存降级兜底
- [x] 战绩查询增强：分页、时间筛选、按房间/名次过滤
- [x] 战绩治理：归档/清理策略（按时间窗口）与索引巡检
- [ ] 段位/积分体系：把当前分数与赛季机制打通
- [ ] 局后复盘：每轮出牌与牌型对比回放
- [ ] 房主控制增强：开局前锁房、踢人、转让房主确认

### B2 后续业务项（默认执行顺序，防上下文丢失）
1. [x] 战绩查询增强（B2）
   - 验收：分页参数可用；支持时间区间/`roomId`/名次过滤；前后端联调通过。
2. [x] 战绩治理与索引巡检（B2）
   - 验收：明确归档/清理窗口；补齐索引并保留巡检文档；清理任务可灰度开启。
3. [ ] 段位/积分体系（B2）
   - 验收：定义赛季周期、积分结算规则、升降段边界；结算与战绩口径一致。
4. [ ] 局后复盘（B2）
   - 验收：支持按局回放关键回合与牌型对比；可从战绩详情入口进入。
5. [ ] 房主控制增强（B2）
   - 验收：开局前锁房、踢人、转让房主流程可用且权限校验完整。

### B3 - 运营能力（中）
- [x] 管理审计日志（关键动作落审计 + 后台查询）
- [ ] 公告与活动配置化
- [ ] 黑名单与风控规则（频繁重连、异常请求）
- [ ] 基础 GM 操作（房间巡检、强制解散）

### B4 - AI 人机与陪练（中）
- [ ] P1 人机补位：减少等待时长，提升开局率
- [ ] P2 AI 对战：单人/双人练习模式 + 难度档（新手/标准/进阶）
- [ ] P3 AI 复盘教练：局后建议、关键决策解释（LLM 非实时链路）
- [ ] AI 公平与风控：信息边界约束、强度校准、异常行为告警

## 6. 前端样式与交互 TODO
### F1 - 视觉体系（高）
- [x] 统一设计令牌：颜色/间距/圆角/阴影/字体/层级
- [x] 场景化主题：大厅、对局、结算三场景视觉区分
- [x] 手牌与公牌组件统一视觉规范（尺寸、状态、动画）

### F2 - 交互细节（高）
- [x] 操作反馈一致：loading/disabled/success/error 统一规范
- [x] 回合节奏动效：发牌、揭牌、结算分数变化动画
- [x] 弱网提示增强：重连进度、重试次数、可恢复提示
- [x] 玩家视角可操作性修复：手牌区置于下方，默认视口内可操作（无需滚动），公牌可读性增强

### F3 - 可用性与适配（中）
- [x] 移动端布局优化（按钮触达、列表密度、弹窗尺寸）
- [x] 可访问性：键盘可操作、语义标签、对比度校验
- [x] 包体优化：按场景分包、重组件按需加载

## 7. 验收基线
- `cd server && npm test` 全绿
- `npm run test:e2e` 全绿
- `npm run build:client` 通过

## 8. 代码审查结论（2026-03-06）
### R1 - Redis 镜像重试缺口（高）
- [x] `RoomRepository` 仅在 `saveRoom/deleteRoom` 抛异常时入重试队列；当返回 `false` 时会被当作成功，可能导致镜像缺口。
- [x] 补齐行为：`save/delete` 返回 `false` 也进入 `pendingMirrorOps`，并补单测覆盖。

### R2 - 网关异常反馈不一致（中）
- [x] `server/app.js` 的消息处理 `catch` 仅服务端打印日志，不给客户端返回标准错误体，前端会等到请求超时才感知失败。
- [x] 补齐行为：统一返回 `400/404/429/500` 错误响应，并附带请求追踪 ID。

### R3 - 入站协议校验不足（中）
- [x] 对 `route/body/id` 增加统一基础校验（长度、类型），异常负载在网关层快速失败。
- [x] 补齐行为：增加字段白名单校验，并记录校验命中指标。

## 9. 工程化交付（2026-03-06）
- [x] 一键启动脚本：`scripts/dev-up.sh` + `npm run dev:all`
- [x] 容器编排：`docker-compose.yml`（server/client/redis/mongo）
